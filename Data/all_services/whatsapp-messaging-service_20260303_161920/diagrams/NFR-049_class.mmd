classDiagram
    class User {
        +String userId
        +String username
        +int storageLimit
        +int currentStorage
        +bool isOverLimit()
    }

    class StorageManager {
        +User[] users
        +void addUser(User user)
        +void removeUser(User user)
        +bool checkStorageLimit(User user)
        +void updateStorage(User user, int amount)
    }

    User "1" --> "1..*" StorageManager : manages